# Simple brainfuck interpreter/transpiler

Require `cc` to be in path.

usage: `bf (--compiled) (--opt) my_program.b`

Interpreted by default, compiled to build folder when `--compiled` is passed.
`--opt` enables `-O2`.

Example programs in bf_src by Daniel B. Cristofani (https://brainfuck.org/).
